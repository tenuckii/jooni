import Canvas from "./Canvas";
import Cursor, { cssCursorByState } from "./Cursor";

const canvasInstance = Canvas.getInstance();
const canvas = Canvas.canvas;
const cursorInstance = Cursor.getInstance();
